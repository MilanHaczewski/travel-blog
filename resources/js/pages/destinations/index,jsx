import AppLayout from '@/Layouts/AppLayout';
import Container from '@/Components/Container';
import { Link } from '@inertiajs/react';

export default function DestinationsIndex({ destinations = [] }) {
    return (
        <AppLayout>
            <section className="bg-sky-50 py-16">
                <Container>
                    <h1 className="text-4xl font-bold text-gray-900">Bestemmingen</h1>
                    <p className="mt-3 max-w-2xl text-gray-600">
                        Bekijk bestemmingen en ontdek de verhalen die erbij horen.
                    </p>
                </Container>
            </section>

            <section className="py-16">
                <Container>
                    {destinations.length > 0 ? (
                        <div className="grid gap-6 md:grid-cols-2 lg:grid-cols-3">
                            {destinations.map((destination) => (
                                <article
                                    key={destination.id}
                                    className="overflow-hidden rounded-2xl border bg-white shadow-sm"
                                >
                                    {destination.cover_image && (
                                        <img
                                            src={destination.cover_image}
                                            alt={destination.title}
                                            className="h-56 w-full object-cover"
                                        />
                                    )}

                                    <div className="p-6">
                                        <p className="mb-2 text-sm text-sky-600">
                                            {destination.country}
                                        </p>

                                        <h2 className="mb-3 text-xl font-semibold text-gray-900">
                                            {destination.title}
                                        </h2>

                                        <p className="mb-4 text-sm text-gray-600">
                                            {destination.description ?? 'Geen beschrijving beschikbaar.'}
                                        </p>

                                        <Link
                                            href={`/destinations/${destination.slug}`}
                                            className="font-medium text-sky-600 hover:text-sky-700"
                                        >
                                            Bekijk bestemming
                                        </Link>
                                    </div>
                                </article>
                            ))}
                        </div>
                    ) : (
                        <div className="rounded-2xl border border-dashed bg-white p-10 text-center text-gray-500">
                            Er zijn nog geen bestemmingen beschikbaar.
                        </div>
                    )}
                </Container>
            </section>
        </AppLayout>
    );
}